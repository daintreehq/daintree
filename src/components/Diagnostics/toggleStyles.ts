/**
 * The pressed look for the diagnostics tabs' toggle and filter buttons (on top
 * of `Button variant="subtle"`). Neutral on purpose: several can be on at once,
 * which rules out accent, and `aria-pressed` carries the state for AT.
 */
export const PRESSED_TOGGLE = "bg-overlay-medium text-text-primary ring-border-strong";
