/**
 * Dims an empty bucket's glyph in both quick state filters — the sidebar's
 * `QuickStateFilterBar` and Pilot's `PilotFilterBar`. The glyph keeps its own
 * hue class; the step lives in `index.css` so it can differ by colour mode.
 */
export const EMPTY_BUCKET_GLYPH_CLASS = "quick-state-glyph-empty";
