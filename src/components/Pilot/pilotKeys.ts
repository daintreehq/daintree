// The All agents footer's key hints, shared with the Daintree Tour's mockup of it.

export function pilotParkKeys(mac: boolean): string {
  return mac ? "⌥↵" : "Alt+↵";
}

export function pilotDrillKeys(mac: boolean): string {
  return mac ? "⌘↵" : "Ctrl+↵";
}
