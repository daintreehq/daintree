// Documentation reference — must NOT trigger the skip.
// `export const RENDERER_API_SKIP = true` is the opt-out used by demo.
export const ALPHA_DOCS_METHOD_CHANNELS = {
  ping: "alpha:ping",
} as const;
