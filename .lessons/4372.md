---
pr: 4372
title: "fix(editor): suppress unhandled rejection in macOS open fallback"
tags: [electron, unhandled-rejection, execa, macos, utility-process]
area: electron/services/
regression_of: null
---

## Context

`EditorService.openFile` on macOS uses `execa` with `{detach: true}` to spawn `open` as a fallback when no editor is configured. The spawned process promise was not given a `.catch()` handler.

## Problem

If the `open` command exits non-zero after `openFile` has already returned (e.g., bad file association, permission denied, corrupted Launch Services DB), the rejected promise floats outside the surrounding `try/catch`. In Electron 37+, unhandled rejections in utility processes cause a hard crash of that process.

## Root cause

`execa` with `{detached: true}` returns a `ChildProcess` promise. When `openFile` returns before the child process exits, any subsequent rejection is unhandled — the original `try/catch` has already exited. The fix was to route every spawn (including the macOS `open` fallback) through a single `launchEditor` helper that attaches `child.catch(() => {})` after `child.unref()`. The Electron 37 change that makes unhandled rejections crash utility processes exposed this latent bug.

## Lesson

All detached `execa` child processes (spawned with `{detached: true}`) must attach a `.catch(() => {})` (here `child.catch(() => {})`) immediately after `child.unref()`, regardless of surrounding try/catch. The try/catch only covers synchronous throws and rejections that happen before the first `await` after function return — it does not protect against post-return rejections from floating promises. Verify the same pattern in every callsite where `execa` is used detached; the durable defense is to funnel all spawns through one helper that owns the `.catch()` (as `EditorService.openFile` now does with `launchEditor`). Note: `shell.openPath()` is still used as the last-resort fallback in `EditorService` (step 5, after the `open -t` path), so it is acceptable as a terminal fallback — just not as the primary spawn path.

## Applies when

- Using `execa` with `{detach: true}` or any fire-and-forget child process
- Platform-specific fallback spawn paths (macOS `open`, Windows `start`, etc.)
- Electron utility processes where unhandled rejections are fatal (Electron 37+)
