---
pr: 4379
title: "fix(terminal): add IME composition guard to xterm key handler"
tags: [xterm, ime, composition, voice-to-text, terminal]
area: src/components/Terminal/
regression_of: null
---

## Context

When using voice-to-text tools (specifically Wispr Flow) to dictate text into a terminal, pressing Enter either did nothing (swallowed) or arrived at the PTY before the composed text, resulting in a blank submission followed by the text appearing on the next line.

## Problem

xterm 6.0's custom key handler (registered via `attachCustomKeyEventHandler` in `src/components/Terminal/XtermAdapter.tsx`) intercepted Enter keypresses without checking `event.isComposing`. During and immediately after IME composition (including voice dictation, which uses the composition API), the Enter keypress should be handled by xterm's built-in `CompositionHelper` to finalize and submit the composed text — not by the custom handler.

## Root cause

The Web Composition API sets `event.isComposing = true` on keyboard events that occur while an IME or voice composition session is active. Custom key handlers in xterm that process Enter without this guard will either consume the Enter before composition finalizes (swallowing it) or deliver it to the PTY before the composed text arrives. The fix: in the `attachCustomKeyEventHandler` callback (which receives the raw `KeyboardEvent`), check `event.isComposing` — and also `event.keyCode === 229` (Chromium's "Process" key signal during active composition, where `isComposing` may not yet be set) — then return early, yielding to xterm's `CompositionHelper`.

## Lesson

Any custom key handler in xterm (`attachCustomKeyEventHandler`) that intercepts Enter (or Shift+Enter) must guard with `if (event.isComposing || event.keyCode === 229) return true`. Track composition state via `compositionstart`/`compositionend` listeners on the terminal element as a backup for rapid cycles where `isComposing` might have already reset (see `src/components/Terminal/hooks/useEditorDomHandlers.ts`, which keeps an `isComposingRef` consumed by the editor keymap in `useEditorKeymap.ts`). This affects not just traditional CJK input but any tool that uses the Composition API including voice-to-text assistants.

## Applies when

- Adding or modifying xterm `onKey` handlers that process Enter/Shift+Enter
- Users report Enter being swallowed or text appearing out of order after voice dictation
- Supporting non-Latin input methods (CJK, Arabic, voice-to-text) in terminal panels
