---
pr: 1705
title: "Preserve browser/webview panel state in dock across show/hide cycles"
tags: [electron, webview, react-portal, dock, panel-lifecycle, state-preservation]
area: src/components/Layout/DockPanelOffscreenContainer.tsx
regression_of: null
---

## Context

Dock panels (browser, dev-preview) host Electron `<webview>` elements. When the dock popover closed, React unmounted the panel subtree, which destroyed the `<webview>` and lost scroll position, navigation history, form inputs, and zoom level. Reopening the dock rebuilt the webview from scratch and reloaded the URL.

## Problem

A webview inside a component that conditionally renders via `{open && <Panel/>}` gets fully destroyed every close/open cycle. No user state survives the cycle because the `<webview>` tag only holds state while attached to the DOM.

## Root cause

Electron `<webview>` has no equivalent of React's `hidden` that preserves its renderer process across unmount. Once the DOM node is removed, the guest page is torn down. Conditional rendering in React therefore cannot be used to "hide" a webview while keeping it alive.

## Lesson

To keep a webview alive while visually hidden, render it into a stable offscreen React portal target that is always mounted (`DockPanelOffscreenContainer.tsx` creates a per-panel offscreen `<div>` slot and always portals the panel into either that slot or the visible popover target, keyed identically so React never remounts). Move the panel between the offscreen container and the visible popover by switching which portal target receives it — never toggle mount state. Hide the offscreen container with `content-visibility: hidden` (skips render work while preserving layout geometry) rather than `display: none`, and keep the container generously sized (the current implementation uses a `position: fixed`, `zIndex: -1`, ~`800x600px` host) — xterm needs a non-trivial layout box to initialize and the webview guest must keep receiving layout. Using a single stable portal target/key for both visible and hidden states avoids the remount that happens when React flips between two different parents.

## Applies when

- Hiding/showing Electron `<webview>` or any element whose teardown cost is non-trivial (iframes with navigation history, canvas renderers, media players)
- Implementing dock/minimize/background-tab semantics where the underlying view must survive visibility toggles
- A panel loses scroll position, form data, or network state every time it is reopened
