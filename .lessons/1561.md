---
pr: 1561
title: "Waiting on utility-process ready without reject handlers hangs app startup"
tags: [electron, utility-process, startup, promise, pty-host]
area: electron/services/pty/PtyHostLifecycle.ts, electron/services/PtyClient.ts, electron/window/windowServices.ts, electron/utils/bootstrapErrorGuard.ts
regression_of: null
---

## Context

`PtyClient.waitForReady()` returned a promise that resolved when the PTY host utility process emitted its `ready` event. On any failure path — `utilityProcess.fork()` throwing synchronously, the host crashing before emitting ready, a missing binary — the promise never settled. The app froze on its splash screen until force-quit. The host-run mechanism (`readyPromise`/`readyResolve`/`readyReject`, the `fork()` try/catch, the `markReady()` late-event guard) now lives in `electron/services/pty/PtyHostLifecycle.ts`; `PtyClient.waitForReady()` is a thin delegate (`PtyClient.ts:350` → `lifecycle.waitForReady()`).

## Problem

The promise had a resolver for the happy path and nothing for failure. Exit events arriving before `ready` were ignored. Late `ready` events after host death were accepted and resolved the promise to a dead client.

## Root cause

Electron utility processes can fail in three distinct ways and each needs an explicit handler:

1. `utilityProcess.fork()` throws synchronously (missing binary, bad args, OS limits)
2. The process starts but crashes before sending its IPC ready message (startup script throws)
3. The process starts, forks OK, but its stdin/stdout pipes fail

If any of these are not wired to reject the ready promise, the awaiter blocks forever.

## Lesson

Every `waitForReady()` over a child/utility process needs all of: (a) synchronous try/catch around `fork()` that rejects (`PtyHostLifecycle.ts:302`/`:334`), (b) an `exit`/`error` handler that rejects if the promise hasn't settled yet (`PtyHostLifecycle.ts:456` — `if (!wasReady && this.readyReject)`), (c) a guard flag that ignores late `ready` events arriving after death (`markReady()` returns `false` when already dead, `PtyHostLifecycle.ts:219`), (d) paired cleanup of both resolve and reject so neither leaks when the promise settles. Pair this with `Promise.allSettled` at the call site so a single service failure degrades the feature area instead of hanging the whole app — in `electron/window/windowServices.ts` the per-window startup awaits `ptyClient.waitForReady()` inside a try/catch (`:260`) and the global startup wraps it in `Promise.allSettled` (`:352`). Also install an early `uncaughtException`/`unhandledRejection` guard inside the utility process before its host module is imported (`electron/utils/bootstrapErrorGuard.ts`): since Electron 37 utility processes only WARN on unhandled rejections, a native-load failure during `await import()` would otherwise leave the process alive but never posting `ready`, so the guard converts that silent hang into a fast `process.exit(1)` that trips the parent's exit handler. Gate dependent work (MessagePort distribution, default terminal spawn) on a per-service `ready` flag.

## Applies when

Reviewing any child process, worker thread, utility process, or remote IPC client that exposes a ready promise. If the code path doesn't reject on fork/spawn errors, exit-before-ready, and already-settled late events, it will eventually hang a user's app. This also applies to WebSocket connections, MessagePort handshakes, and anything with a "wait for first message" contract.
