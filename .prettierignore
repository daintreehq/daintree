# Build outputs
dist/
dist-electron/
dist-typecheck/
release/

# Dependencies
node_modules/

# Local tooling / session artifacts (not project source)
.claude/

# Vendored submodule: the daintree-assistant engine is a separate repo with its own
# formatting and CI. Reformatting its files here would create a diff it can never
# accept back.
vendor/

# Package manager
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build config
*.config.cjs

# Generated files
*.min.js
*.min.css

# Generated plugin view — `npm run build:sample-rich-view` output, committed and
# shipped verbatim (#11208). Formatting it here would make every regeneration
# fail format:check. Its hand-authored sibling (rich-panel-view.js) IS formatted.
plugins/sample/rich-daintree/view/hook-panel-view.js

# Generated plugin view — `npm run build:sample-file-tree` output, committed and
# shipped verbatim. Same reason as above: `check:sample-views` regenerates it and
# compares byte-for-byte, so formatting it here would make every regeneration
# fail one gate or the other.
plugins/sample/file-tree/view/file-tree-view.js

# Generated API-surface snapshots (raw tsup .d.ts output — see scripts/ci/check-api-surface.mjs)
packages/*/api-report/

# Generated shortcut reference — `npm run codegen:keybindings` output, compared
# byte-for-byte by check:keybindings. Formatting it here would make every
# regeneration fail that check.
docs/keyboard-shortcuts.md

# Generated manifest JSON Schemas — `npm run codegen:plugin-schema` output,
# compared byte-for-byte by check:plugin-schema. Same reason as above: the
# generator emits JSON.stringify's shape, so formatting them here would make
# every regeneration fail one gate or the other.
schemas/plugin.schema.json
schemas/plugin.project.schema.json

# Generated action reference — `npm run codegen:plugin-actions` output, compared
# byte-for-byte by its test in src/services/actions/__tests__. Same reason as
# the shortcut reference above.
docs/plugins/actions.md

# Assets
build/
*.ico
*.icns
*.png
*.jpg
*.jpeg
*.gif
*.svg
*.woff
*.woff2
*.ttf
*.eot
